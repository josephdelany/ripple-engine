> **ARCHIVED — HISTORICAL RECORD, NOT A CURRENT CLAIM.** Adversarial review findings, kept as evidence. Preserved for audit; current release status is in [`SUBMISSION_STATUS.md`](../../SUBMISSION_STATUS.md).

# D-8 — Paper review, sentence by sentence

Reviewer: adversarial, read-only. Method: `docs/PAPER_DRAFT.md` (486 lines) was
split into 254 numbered sentences with a line map by
`python3` (script and output kept in this scratchpad: `sentences.txt`). Every
numeric claim below was located in its source JSON/CSV/MD file and read by
`python3` (`json.load` / `csv.reader` / `sqlite3`), never by eye. Full dumps of
`data/walk_forward/summary.json` (run `walk_20260902T182828Z`) are preserved in
this scratchpad (`extract2.txt`, `extract3.txt`) because — see Finding 6 — the
live file no longer contains that run.

**Legend:** MATCH (exact) · ROUNDED (both values given) · STALE (right file,
now-superseded value) · UNTRACEABLE (no file gives it) · WRONG.

---

## (a) Every numeric sentence, checked against its source

| # | line(s) | claim (quoted/paraphrased) | source | file value | status |
|---|---|---|---|---|---|
| 2 | 3-8 | "run `walk_20260902T182828Z`" | `data/walk_forward/summary.json` (as captured before overwrite) | `run_id` = `walk_20260902T182828Z` | MATCH |
| 7 | 14-43 | "313 dated geopolitical and policy shocks (1973–2026)... seventeen open... datasets" | `oil.db.events` count; front matter | 313 rows, dates 1973-10-06→2026-06-17 | MATCH (313); "seventeen datasets" is a source-list count, not independently falsifiable from one query — see §3 sources list, MATCH by enumeration |
| 9 | 14-43 | escalation Brier skill −0.007, CI [−0.084,+0.065], SPA p=0.79; price CRPS skill −0.028, CI [−0.062,+0.008] | `tiers.daily.G.engine_vs.climatology`; `tiers.daily.G.spa`; `tiers.daily.P.engine_vs.climatology` | skill −0.0069896 CI[−0.08406,+0.06465]; p_spa 0.79321; skill −0.0278535 CI[−0.06217,+0.00847] | MATCH (rounded) |
| 10 | 14-43 | "beats persistence on price (+0.163, p<0.001)" | `tiers.daily.P.engine_vs.persistence` | skill 0.163234, dm_p 1.1785e-11 | MATCH |
| 11 | 14-43 | permutation p=0.008 | `permutation.p_value` | 0.0079920... | MATCH |
| 12 | 14-43 | "35% have no identifiable event" | `data/big_moves/brent.json` | no_identified_event 15 / n_episodes 43 = 34.9% | MATCH (rounded); see Finding 2 on how the 43 episodes were built |
| 13 | 14-43 | "escalation skill +0.12 against self-coded labels" | none named | no Appendix A row | **UNTRACEABLE** — see Finding 3 |
| 31 | 98-104 | class counts: sanctions 57, policy 57, conflict 55, OPEC 52, infrastructure 48, chokepoint 27, demand 17; 313 total; 1973-10-06…2026-06-17 | `oil.db.events` (`SELECT type, COUNT(*) ... GROUP BY type`) | 57/57/55/52/48/27/17, sum 313, date range exact | MATCH (executed via sqlite3, exact) |
| 32 | 98-104 | "187 are geopolitical" | `data_state.n_geo` | 187 | MATCH |
| 34 | 106-108 | Brent from 1987-05-20, WTI from 1986-01-02, ~598 series | `data/big_moves/brent.json.first`="1987-05-20" | MATCH (Brent date); WTI/598 not independently re-verified (not in files reviewed) | MATCH (Brent), not verified (WTI start, 598 count) |
| 37 | 110-121 | "Seventeen loaders populate 34 of 70 registered fields" | `data/state/status.json`; `data/acceptance_dod.json` D2 | **now**: `fields_loaded`=49, `fields_registered`=70; D2 note: "27 loaders... 49 loaded fields" | **STALE** — see Finding 4 (true at commit time, false now) |
| 43 | 127-132 | Big Move definition: top-5%, 20/60-day windows, onset=extreme, attribution −7d to end, ANTICIPATED if event lag >20d | `BIG_MOVES_REGISTRATION.md` rules 1-3, Amendment 2 | text matches the registration document's rules | MATCH (description); **but see Finding 2** — the code that produced the numbers below does not implement "60 trading days" clustering as registered |
| 46 | 143-152 | Brent ratios: demand 1.61, OPEC 1.61, policy 1.85, infrastructure 0.62, sanctions 0.79, chokepoint 0.84 | `data/big_moves/brent.json` `p_big_given_class`, `everyday_base_rate_pct` | recomputed: demand 5/17÷0.183=1.607; OPEC 15/51÷0.183=1.607; policy 19/56÷0.183=1.854; infra 5/44÷0.183=0.621; sanctions 8/55÷0.183=0.795; chokepoint 4/26÷0.183=0.841 | MATCH (all, rounded to 2dp, executed) |
| 47 | 143-152 | diesel-crack ratios: chokepoint 2.29, conflict 2.62, infrastructure 1.98, sanctions 2.06 | `data/big_moves/diesel_crack.json` | recomputed: 10/26÷0.168=2.289; 22/50÷0.168=2.619; 15/45÷0.168=1.984; 19/55÷0.168=2.056 | MATCH (all, executed) |
| 51 | 154-157 | "n per class is small (4–19 episodes)" | `data/big_moves/*.json` `p_big_given_class` numerators | class n's across assets range 4 (chokepoint, brent num.) to 19 (sanctions, diesel crack numerator counted against n=55 pool… ) | MATCH (plausible range from the tables; not exhaustively min/maxed across all 4 files) |
| 53 | 161-168 | κ vs ICB −0.001 (n=43), vs MID −0.234, vs UCDP 0.104 | `data/state/outcomes_kappa.json` | icb: −0.0013, n=43; mid: −0.2342, n=15; ucdp: 0.104, n=184 | MATCH (rounded; n=43 correctly scoped to ICB only, not a blanket n) |
| 58 | 170-180 | "184 of 187... labelled (98.4%); 3 have no independent covering source" | `data_state` | n_geo_with_ies90_level 184, n_geo_no_independent_outcome 3, share_geo_labelled 0.984 | MATCH |
| 59 | 170-180 | level counts 0→76,1→6,2→48,3→54; 95 deal | `data_state.ies90_level_counts`, `n_geo_with_deal_flag` | 76/6/48/54; 95 | MATCH |
| 60 | 170-180 | "30-event audit sheet... awaits the author's row-by-row check" | `data/audits/ies90_audit_30.csv` | 30 unique `event` rows, `joe_check` column empty on all 30 | MATCH (executed check) |
| 66 | 184-196 | "Twelve registered weightings... η=0.25" | `data/walk_forward/menu.json`-equivalent (`summary.menu`); `registered.eta` | 12 menu items listed; eta 0.25 | MATCH — **but** η is registered only as a Python constant (`src/walk.py` `LN.ETA`), not stated as a number in `WALK_FORWARD_PROTOCOL.md`'s text — see Finding 7 |
| 69 | 200-216 | "seal_check.ok = true, 1,565 records" | `seal_check` | ok true, n_records 1565 | MATCH |
| 71 | 200-216 | "four baselines... persistence (price; escalation added by Amendment B)" | `tiers.daily.G.engine_vs` keys; `WALK_FORWARD_PROTOCOL.md` Amendment B | G has 3 baselines (climatology, frozen, random_analogs) in this run; Amendment B (G-persistence) is dated 2026-09-02 in the protocol but **not applied to this run's G scores** | MATCH as literally written (the paper discloses the exception) — but see Finding 8 for how the Abstract elides this |
| 72 | 200-216 | cluster 35d, mean block 2.32; boot 2,000; SPA boot 1,000 over 14 models; perm 1,000; placebo 5 reps/30d excl.; spec-curve 162 cells | `tiers.daily.dependence`; `registered.{n_boot,n_spa_boot,n_perm,placebo_reps,placebo_excl_days}`; `spec_curve.n_specs` | cluster_days 35, mean_block 2.32; n_boot 2000; n_spa_boot 1000 (14 models incl. engine+frozen); n_perm 1000; placebo_reps 5, excl 30; n_specs 162 | MATCH (all) — again, most of these are code constants, not protocol-document text; see Finding 7 |
| 76 | 227-229 | "299 reads, 253 scored... 150 with an escalation label" | `tiers.daily.{n_reads,n_scored_burn_in}`; `G.engine_vs.climatology.n` | 299, 253, 150 | MATCH |
| 77 | 227-229 | "monthly tier has 14 reads and 0 scored" | `tiers.monthly.{n_reads,n_scored_burn_in}` | 14, 0 | MATCH |
| 78-84 | 231-237 | G Brier 0.706/0.701, skill −0.007 CI[−0.084,+0.065] p=0.85; log skill −0.035; vs frozen −0.003; vs random +0.062 p=0.068; SPA M07 p_RC .88 p_SPA .79; DEAL −0.218 n66 base 6%; per-class +0.017/−0.065/+0.018/+0.019 | `tiers.daily.G.*` | engine_mean 0.70596, ref 0.70106, skill −0.0069896, CI[−0.08406,+0.06465], dm_p 0.8471; log skill −0.035054; frozen skill −0.0026096; random skill 0.062242 p 0.067742; spa p_rc 0.88312 p_spa 0.79321; deal skill −0.217918 n66 base .0606; per_class chokepoint .016996 conflict −.064612 infra .018052 sanctions .019421 | MATCH (all, rounded) |
| 85 | 239-242 | RPS vs climatology +0.072 CI[−0.008,+0.151] p=.076; vs random +0.140 CI[.061,.219] p<.001 | `tiers.daily.G.rps.engine_vs` | climatology skill .071778 CI[−.007588,.151177] p .076145; random skill .139828 CI[.061108,.219050] p .000537 | MATCH |
| 87-93 | 244-250 | P CRPS skill −0.028 CI[−.062,+.008] p=.14; vs persistence +.163 CI[.121,.210] p<.001; vs random +.035 p=.053; vs frozen +.001; pinball 10/50/90 −.039/+.004/−.073 CI[−.149,−.017]; PIT χ²=16.9/9df, first bin 41 vs 25.3, climatology 13.3; sign 0.498; SPA p=.94 | `tiers.daily.P.*` | crps skill −.027854 CI[−.062174,.008467] p .136303; persistence skill .163234 CI[.121401,.209912] p 1.18e-11; random skill .034962 p .052678; frozen skill .001305; pin10 skill −.038537; pin50 skill .004480; pin90 skill −.073143 CI[−.148843,−.017314]; pit_engine chi2 16.8735 counts[0]=41 expected 25.3; pit_climatology chi2 13.3478; sign_accuracy .498; P.spa p_spa .93706 | MATCH (all, rounded) |
| 94 | 252-253 | M precision .337 recall .544 base .225 n=253 | `tiers.daily.M.engine` | precision .337 recall .544 base_rate .225 n 253 | MATCH |
| 96-98 | 260-262 | regime blocks: drop2008 G−.013 P−.028; drop2020 G−.014 P−.011; drop2026 G−.007 P−.027 | `regime_blocks.*` | drop_2008 G −.013396 P −.028136; drop_2020 G −.014320 P −.010591; drop_2026 G −.006990 P −.027462 | MATCH |
| 100 | 264-266 | spec curve: median −.023 IQR[−.041,−.007] max +.015 pos 16.7% | `spec_curve.skill_distribution` | median −.022894 p25 −.040970 p75 −.007154 max .014629 share_positive .16667 | MATCH |
| 102-103 | 268-272 | placebo vs random −.024 CI[−.053,+.007] covers 0; vs climatology −.081 CI excl. 0; fair −.008 CI covers 0 | `placebo.{vs_random_analogs,vs_climatology,fair_vs_climatology}` | −.023650 CI[−.053347,.006774] covers_zero true; −.080821 CI[−.11230,−.04760] covers_zero false; −.008023 CI[−.04278,.02757] covers_zero true | MATCH (values) — **but "the placebo is null, as required" is only true under the unratified reference-class choice; see Finding 1** |
| 104 | 274-277 | permutation null mean −.061 SD .025 p95 −.018; observed +.0005 p=.008 | `permutation.*` | null_mean −.060566 null_sd .024953 null_p95 −.018412 observed_skill .0005166 p .007992 | MATCH |
| 106-107 | 279-283 | 32 comparisons; only vs-persistence survives; M02 q=.024, M06 q<.001 survive on P vs climatology | `fdr.family` | 32 entries; only `daily:P:engine_vs_persistence` survives=true besides M02 (q .02411) and M06 (q .00019) | MATCH |
| 108 | 285-287 | leakage: 313 reads differ; G .706→.619; P 7.92→8.04 | `leakage_test.*` | n_reads_with_different_analogs 313; G sealed .70596 broken .619035; P sealed 7.91858 broken 8.040566 | MATCH |
| 110 | 289-294 | learning curve ends G −.007 at n=150; RPS ends +.072 | `tiers.daily.G.engine_vs.climatology`; `rps.engine_vs.climatology` | endpoints match (−.006990, .071778) | MATCH (endpoints only — the intermediate claims "first turns positive 2021-03-07," "drifts −.01/+.04 through 2022-2023," and "frozen tracks within 0.007 at every point" sit inside `learning_curve` arrays that were **not re-extracted before the live file was overwritten (Finding 6)** — not independently verified, flagged as thin |
| 113-114 | 298-306 | Murphy terms: no-escalation resolution .038 vs climatology .0004, reliability .002; force/war reliability .042/.035, 0.4-1.0 bin freq 0-36% | `tiers.daily.G.murphy_engine/murphy_climatology` | class "0": engine resolution .03762 reliability .00235; climatology resolution .00041; class "2" reliability .04201; class "3" reliability .03512; bin 0.4-0.8 observed_freq for class 2 = 0.0 (0.6-0.8 bin) and up to .36 elsewhere in nearby bins — reasonable paraphrase | MATCH (rounded; "0-36%" is a defensible summary of the diagram's spread) |
| 122-124 | 317-343 | 150 escalation + 253 price reads; CI ±0.07; 624 candidate records | `tiers.daily.{n_scored_burn_in}`; `G.engine_vs.climatology.ci95` half-width; `pre1987_candidates.csv` | 253 P-scored, 150 G-scored; CI half-width ≈(.0847-(-.0641... using [−.084,.065]) ≈0.075≈"±0.07"; csv rows−header = 624 | MATCH (all) |
| 127 | 317-343 | fair Brier +.057 CI[−.019,.129]; fair CRPS +.027 CI[−.009,.078]; fair RPS +.139 CI[.058,.222] | `tiers.daily.G.diagnostic_fair`; `tiers.daily.P.diagnostic_fair` | G brier_fair engine_vs_climatology skill .056748 CI[−.018574,.128723]; P crps_fair skill .026610 CI[−.008756,.078103]; G rps_fair skill .139467 CI[.058122,.221626] | MATCH |
| 145 | 359-372 | "Pre-registration files with git timestamps... every amendment dated and appended" | git log | `WALK_FORWARD_PROTOCOL.md`/`OUTCOME_MAPPING.md` registered ahead of their runs; **`BIG_MOVES_REGISTRATION.md` and `CLAIM_LEDGER_REGISTRATION.md` committed in the same commit (594d2fa) as their code and output data** | **WRONG (for 2 of 6 named files)** — see Finding 5 |
| 146 | 359-372 | H1 reproduced exactly at +5.56pp; fell inside placebo band; all "validated" claims downgraded | `docs/red_team_1.md` Part 2 (R1, R2, R7) | R1: raw +5.5615pp [.9436,9.6525] "reproduces exactly"; R2: real +5.56pp "lies inside the pseudo band"; R7: "Validated set... empty" | MATCH |
| 147 | 359-372 | "escalation skill +0.12, p<0.001... scored against self-coded labels... did not survive independent ones" | none named in Appendix A | nearest source `data/gates/step8_2026-09-02.md`: "+0.116... (p = 0.001)" from run **`walk_20260902T180821Z`**, a different run than 182828Z | **UNTRACEABLE / STALE** — see Finding 3 |
| 150 | 359-372 | "322 tests" | `data/acceptance_dod.json` D1 | **now**: "354 passed, 13 skipped" | **STALE** — see Finding 4 |

### Appendix A audit (every row checked against the path it names, executed)

All 26 rows name an existing path. Two rows disagree with the **current** content
of the file they name (see Finding 4): the "34 of 70 fields; 17 loaders" row
(`data/state/status.json`; `data/acceptance_dod.json` D2 — now 49/70, 27 loaders)
and the "322 tests" row (`data/acceptance_dod.json` D1 — now 354). One row names
no path at all for a number that appears in the prose it is supposedly footnoting
— the +0.12 escalation figure in the Abstract and §13 has **no Appendix A row**,
making it the one number in the draft that is not traced by the mechanism the
draft's own front matter promises ("Every number in this draft is traced in
Appendix A"). Every other row's path exists and its value matches the run
`walk_20260902T182828Z` content as captured in this review (see Finding 6 on why
"exists and matches" required capturing the run before the live file moved on).

---

## (b) Every causal/epistemic word, judged against the record

| line | quoted sentence (trimmed) | word | supported? | why |
|---|---|---|---|---|
| 30-31 | "A label-permutation test nonetheless **rejects** the hypothesis that the engine's analog selection is noise (p=0.008)" | rejects | Technically supported (p=0.008 is the permutation test's own output) but **the strength is not disclosed as fragile**: this p is outside the FDR family (32-comparison BH family in §9/Appendix A `fdr.family` does not include the permutation test), so it has not been corrected for the same multiplicity the paper corrects everything else for. Also see the IES-90 war-contamination point below — "the analog assignment is exchangeable with a random one" is a narrower claim than the prose built on it (sentence 105). |
| 32-33 | "...so what structure exists is destroyed by miscalibration" | (implicit causal — "destroyed") | Supported by the Murphy-decomposition numbers themselves (reliability terms 0.035-0.042 swamping resolution 0.0026-0.0088 on force/war), correctly reported | — |
| 40-43 | "We argue the null is informative... does not yet cash out as a calibrated forecast" | argue/informative | Appropriately hedged ("we argue") | — |
| 35, 338 | "...finds that 35% have no identifiable event" / "finds 35% of big Brent moves have no corpus event" | finds | The 35% figure itself reproduces from the file (Finding — matches). But "finds" implies the number is a fixed, discovered fact; it is in fact a function of an unregistered clustering choice (Finding 2: cluster_days=90/merge_days=60 vs the registered 60/no-merge) — a different, registered run could show a different episode count and a different %. Not disclosed. |
| 277 | "**The engine's analog selection carries information about the labels.**" | carries information (causal/epistemic claim) | **Not fully supported** — see Finding 10. IES-90 "war" (level 3) labels are heavily pre-existing-war-contaminated; situation-similarity retrieval trivially clusters already-at-war dyads together, so "carries information about the labels" is consistent with a simpler, non-forecasting explanation (the retrieval is finding *current conflict status*, not *future escalation*) that the paper does not rule out or mention. |
| 303 | "The engine **knows** something about which situations stay quiet and is over-confident about which ones burn." | knows (anthropomorphic epistemic claim) | Same caveat as above — "knows" attributes forecasting insight to what may be class-base-rate leakage through the war-contaminated label. The resolution term on the no-escalation class (0.038) is real and reported correctly, but the paper's language claims more understanding than the record isolates. |
| 298 | "The two results are not in tension; the Murphy decomposition **says why**." | explains (implicit) | Supported as an internally consistent decomposition of the same Brier score; correctly framed as decomposition, not new evidence. |
| 108-109 | "...moves the sealed G score from 0.706 to 0.619... The filtration **binds**." | binds (causal claim about the mechanism) | Supported — this is exactly what the leakage test is designed to show, and the numbers check out (0.706→0.619, 7.92→8.04). |
| 102 | "...the placebo is null, **as required**." | null / as required (validates a gate) | **Not fully supported** — see Finding 1. "As required" implies the registered protocol's requirement is met; the requirement is met only under an unratified amendment's redefinition of the reference class. Against the plain climatology reference used everywhere else in the document, the placebo is not null (CI excludes zero). |
| 311-313 | "If the diagnosis is right, M13's Brier skill **will be** positive... The result will be published either way." | will (future prediction, appropriately hedged as a pre-registered test) | Appropriately hedged — framed as a testable diagnosis, not a claim. |
| 42 | "...**would** change that verdict" (§12 heading) | would | Appropriately hedged — a conditional, not a claim. |

No instance of "shows," "demonstrates," "proves," "validates" (as a verb applied to a claim), "confirms," "drives," "leads to," "predicts," "suggests," or "indicates" appears in the paper body applied to a result stronger than the record supports; the paper is generally disciplined about hedging its skill claims (correctly reports the null verdict throughout §8-§12). The two places it allows itself unhedged, structural language — the permutation-test paragraph (§9) and the Murphy-decomposition paragraph (§10) — are exactly the two places a known, disclosed-elsewhere data problem (IES-90 war-label contamination) is not mentioned, and the placebo-null claim (§9) is exactly the one place a load-bearing methodological choice (Amendment A.4's reference class) is stated as settled ("as required") rather than as contingent on Joe's still-pending ratification.

---

## (c) Methods-vs-code discrepancies

1. **Amendment A to `WALK_FORWARD_PROTOCOL.md` is a proposal, not a registered amendment.** `data/gates/step8_2026-09-02.md` presents "Amendment A" (G target = IES-90, the RPS score, the placebo reference class A.4, the size-corrected-diagnostics framing A.5) as text "ready to append," explicitly gated as "Gate 1 (Joe): ratify Amendment A." `WALK_FORWARD_PROTOCOL.md` itself contains Amendments **B through I** — no Amendment A. (`grep -n -i "amendment a" WALK_FORWARD_PROTOCOL.md` returns nothing; `grep -n "^## Amendment" WALK_FORWARD_PROTOCOL.md` lists B, C, D, E, F, G, H, I only.) The run the paper reports (182828Z) was nonetheless computed under Amendment A's rules — the G target is IES-90 (not the protocol's still-literal "+90d branch label"), and the placebo is judged against A.4's reference class. The paper's abstract and §1 call this "a pre-registered walk-forward protocol" and "a rule written before any number was computed" without flagging that its central definitions rest on an unratified proposal. (Note, separately: `WORLD_STATE_FRAMEWORK.md`'s own "Amendment A," cited correctly in §3, *is* registered in that document at line 164 — this is a different Amendment A in a different file and is not the issue.)
2. **Big Moves clustering does not match its own registration for the daily tier.** `BIG_MOVES_REGISTRATION.md` rule 3: "consecutive qualifying dates within **60 trading days**... belong to that episode" — no merge step for the daily tier (merge is registered only for the monthly tier, Amendment 3, "same-sign merge within 180 days"). `src/big_moves.py` line 33: `dict(windows=(20, 60), cluster_days=90, merge_days=60, ...)` for `"daily"`. Every daily-tier Big Moves number in the paper (43/46/36 episodes, the 35%/30%/19% "no identified event" rates, every per-class ratio) was produced with `cluster_days=90` and an unregistered `merge_days=60` step, not the registered 60-trading-day rule.
3. **G had three baselines in run 182828Z; the fourth (persistence) was defined by Amendment B but is not reflected in this run's `G.engine_vs`.** `tiers.daily.G.engine_vs` has exactly `climatology`, `frozen`, `random_analogs`. The paper discloses this correctly at §7 ("persistence (price; escalation added by Amendment B)") but the Abstract's "compared to four baselines" (unqualified) reads as if all four apply to both targets in this run.
4. **"Sealed" is accurately described.** `seal(record)` (`src/walk.py:91-94`) computes SHA-256 over the whole record (which includes `run_id`, set at construction) plus `sealed_at` (set inside `seal()` before hashing) — matches the paper's "sealed by SHA-256 over its canonical payload and its `sealed_at` timestamp." No discrepancy.
5. **Several "registered" numeric knobs exist only as code, not as protocol-document text.** `WALK_FORWARD_PROTOCOL.md`'s prose gives explicit numbers for burn-in (8), cluster window (35 days), and permutation count (1,000 times) — all three do appear in the document. It does **not** state η (the Hedge learning rate), `n_boot` (2000), `n_spa_boot` (1000), `random_draws` (25), `placebo_reps` (5), or the specification-curve grid values anywhere in its text; these live only in `src/walk.py`'s `REGISTERED` dict (lines 56-77), each with an inline comment pointing at a protocol section number but no independently-dated textual commitment to the specific value. A reader auditing "was this number fixed before results were seen" can verify it only via `git blame` on the code, not via the dated document the paper cites as the registration.
6. **`BIG_MOVES_REGISTRATION.md` and `CLAIM_LEDGER_REGISTRATION.md` landed in the same commit as their implementing code and pre-computed output.** `git show --stat 594d2fa` shows `BIG_MOVES_REGISTRATION.md`, `CLAIM_LEDGER_REGISTRATION.md`, `src/big_moves.py`, `src/materiality.py`, `data/big_moves/{brent,diesel_crack,wti}.json` (already computed) all added together. Git timestamps establish an ordering *within* the commit only by document convention, not by commit history, for these two files — unlike `WALK_FORWARD_PROTOCOL.md` and `OUTCOME_MAPPING.md`, whose amendments do precede (by minutes-to-hours, per `git log`) the runs that use them.
7. **"322 tests" and "34 of 70 fields / 17 loaders" were true at the moment `docs/PAPER_DRAFT.md` was committed and false minutes later**, within the same working session (see Finding 4/(a) Appendix A audit above; exact commit hashes and timestamps in Finding 4 below).

---

## (d) Demo check results (docs/demos/{1990,911,2026}.md vs reads.jsonl / scores.jsonl, run 182828Z)

None of the three demos is cited anywhere in `docs/PAPER_DRAFT.md` (checked: no match for "demo," "1990-08," "kuwait," "9/11," or any hash prefix in the paper text — the only cross-reference runs the other way, `docs/demos/911.md` line 47 cites "§10 of the paper"). They were still checked per the task's scope.

**1990 (Iraq invades Kuwait), hash `5bc0293dd2d9…`:** every field checked — `sealed_at` (18:28:28.970063 vs quoted .970Z), `n_pool`/`n_pool_g`/`n_pool_p` (7/7/2), `state_unknown` (7 fields, exact list and order), `engine.G` (.44649/.0/.29661/.25689 → .446/.000/.297/.257), all four `G_atoms` weights and labels, `engine.P` atoms and p10/50/90, materiality (`NOT_MATERIAL` ×12), the outcome (`level` 3, `chg_pct` 23.5955→+23.6%, `closed_on`, `in_big_move`), and the full score table (engine/climatology/persistence Brier/RPS/CRPS) — **all MATCH exactly** (rounded to the demo's stated precision). No finding.

**911 (September 11), hash `8b7277ff28fc…`:** `sealed_at` .002732→.003Z; `looked_up_at` .003819 (gap = 1.087ms, matches "the sealing timestamp precedes the outcome lookup by one millisecond"); `n_pool`=10, `burn_in_ok`=True; `state_unknown` (8 fields, exact list/order); `engine.G` (.26059/.0/.28668/.45274 → .261/.000/.287/.453); all nine `G_atoms` weights/labels including the two long-tail ones (.03054→"0.03", .009119→"0.01"); `engine.P` five atoms and p10/50/90; materiality `MATERIAL`×12; outcome (`level` 3, `chg_pct` −29.0865→−29.1%); full score table incl. sign accuracy (✓/✓/✗) — **all MATCH exactly**. All nine analog `event_date`s independently verified < 2001-09-11 via `oil.db`. No finding.

**2026 (Hormuz closure), hash `aed201938e98…`:** `sealed_at` .630288→.630Z; `looked_up_at` .632458 (gap 2.17ms — table doesn't quote a gap, no claim to check); "25 prior events in the... pool" matches `n_pool_g`/`n_pool_p`=25 (not the broader `n_pool`=26 — consistent, not a discrepancy); `state_unknown`=[] (empty, matches "no unknown fields"); `engine.G` (.42862/.12797/.28186/.16158 → .429/.128/.282/.162); 14 `G_atoms`, top-4/next-3/remaining-7 groupings and rounded weights all match; materiality (`NOT_MATERIAL`×11, `M02_situation_only` `MATERIAL`) exact; outcome `chg_pct` 46.5915→+46.6%, `no_independent_outcome`=true; price-only score table (engine/frozen/climatology/random/persistence × CRPS/pin90) — **all MATCH exactly**.

  - **One finding: the "largest atom" is misattributed.** The demo states: *"The largest atom was Ever Given at +12%."* In the sealed read, the atom valued **+12.011%** (which is what the quoted p90=+12.0 and the "largest atom" language refer to) belongs to **`hankuk_chemi_seizure_2021`**, not `suez_ever_given_2021`. `suez_ever_given_2021`'s actual price atom is **+8.522%**. Both events are in the same read's 14-atom pool (`engine.P.ids`/`engine.P.values`, parallel arrays, index-checked); the magnitude quoted is correct (it is in the record) but attached to the wrong event name. The floor atom, correctly attributed ("Libya 2014 at −25%" ↔ `libya_ports_clashes_2014` = −24.996%), shows the error is a mislabel, not a magnitude error.

---

## (e) FINDINGS, ranked by whether they change a published sentence

**1. The Abstract/§9's placebo "null" claim is not supported by the paper's own registered protocol text — only by an unratified amendment.**
> §9: *"...the placebo is null, as required."*
> §1/Abstract imply the whole test runs "under a pre-registered walk-forward protocol... a rule written before any number was computed."

`WALK_FORWARD_PROTOCOL.md` §6 (as actually written, unamended) says only: *"engine skill on placebos must be indistinguishable from zero"* — no reference class is specified in that sentence, and the document's only other stated default reference is climatology (§4 baseline 1, "the honest bar"; §7's VALIDATED test is explicitly "skill > 0 against climatology"). Against climatology, the placebo skill is **−0.081, CI [−0.112, −0.048]** — CI excludes zero, i.e. **not** null. The paper's "null, as required" reading is licensed only by **Amendment A.4** ("the placebo reference... is judged against §4 baseline 3 [random analogs]... because it is size-matched"), which exists solely as a proposed, unratified block of text inside `data/gates/step8_2026-09-02.md`, explicitly labeled "Gate 1 (Joe): ratify Amendment A" — still awaiting Joe's decision, and `WALK_FORWARD_PROTOCOL.md` (the document the paper cites for this exact rule) contains no Amendment A at all. The size-corrected "fair" reading the paper also cites (−0.008, CI covers zero) is explicitly, in the same protocol machinery, "diagnostic, not registered" and "the decision whether to register the fair scores... is the author's [Joe's]." The `verdict.rules["engine:P"].conditions.placebo_null = true` flag baked into `summary.json` — which the paper's Verdict section (§8) inherits — is computed under this same unratified definition.

**2. Every Big Moves number in the paper (43/46/36/18 episodes, 35%/30%/19%/— "no identified event," every P(big|class) ratio) was computed with clustering parameters that do not match the paper's own cited registration.**
> §4: *"clustering: consecutive qualifying dates within 60 trading days of an episode's start belong to that episode"* is what `BIG_MOVES_REGISTRATION.md` (which §4 cites, Amendments 1-3) actually specifies for the daily tier, with no merge step.
> The code that produced every number in §4's table and the Abstract's "35%" (`src/big_moves.py` line 33) uses `cluster_days=90, merge_days=60` for the daily tier — a wider clustering window than registered, plus an unregistered merge pass.

**3. The "escalation skill +0.12 (p<0.001) against self-coded labels" figure (Abstract; §13) has no Appendix A row and, as best traced, belongs to a different, earlier walk run than the one the draft says all its numbers come from.**
> Abstract: *"Two earlier positive headlines (volatility-stress amplification; **escalation skill +0.12** against self-coded labels) did not survive..."*
> §13: *"A second headline (**escalation skill +0.12, p < 0.001**) was scored against self-coded labels and did not survive independent ones."*

No Appendix A row cites a source for this number (checked: 26 rows, none mentions it). The nearest match, `data/gates/step8_2026-09-02.md` ("For Joe's reading of the published run (**walk_20260902T180821Z**)... Against the retired corpus-derived label it had read **+0.116** (p = 0.001)"), is from run `180821Z`, roughly 20 minutes before the paper's own stated run `182828Z`, and gives 0.116/p=0.001, not the paper's rounder "+0.12/p<0.001." `README.md` (line 68-69) makes the same claim but at least attributes it to "an earlier run" — the paper drops that qualifier and presents the number as part of the same evidentiary record as everything else in §13.

**4. Two Appendix A rows no longer match the files they cite — not because they were ever wrong, but because those files are mutable and were updated minutes after the paper was committed.**
> §3: *"Seventeen loaders populate 34 of 70 registered fields (`data/state/status.json`)."*
> §13: *"322 tests..."*

`git log` shows: `docs/PAPER_DRAFT.md` committed at `0a2f9ed`, 2026-09-02 15:39:01 -0400. "322 tests" was accurate as of the immediately preceding commit `287eaf3` (14:55:04) and was overwritten by `86282ec` (15:54:11, 15 minutes *after* the paper) to "354 passed, 13 skipped." "34 of 70 fields" was accurate at paper-commit time and was overwritten 4 minutes later by `266b918` (15:43:46, commit message: *"world-state panel 34 -> 50 of 70 fields"*); `data/acceptance_dod.json`'s current D2 note reads *"27 loaders (distinct sources behind 49 loaded fields)... fields loaded 49 / registered 70."* The paper's Appendix A claims these numbers are "traced... to the file that produced it" as though the pointer were stable; it is not — both files are regenerated by later, unrelated work in the same session, with no run-scoping to pin them to the draft's stated numbers.

**5. `BIG_MOVES_REGISTRATION.md` and `CLAIM_LEDGER_REGISTRATION.md` cannot demonstrate ex-ante registration from git history — they landed in the same commit as their code and already-computed output.**
> §13: *"Pre-registration files with git timestamps (`BRIEF_SKELETON.md`, `PRE_REGISTRATION_V2.md`, `BIG_MOVES_REGISTRATION.md`, `CLAIM_LEDGER_REGISTRATION.md`, `OUTCOME_MAPPING.md`, `WALK_FORWARD_PROTOCOL.md`), every amendment dated and appended."*

Commit `594d2fa` adds `BIG_MOVES_REGISTRATION.md`, `src/big_moves.py`, and the already-computed `data/big_moves/{brent,diesel_crack,wti}.json` together, and likewise `CLAIM_LEDGER_REGISTRATION.md` with `src/materiality.py`/`src/ledger.py`. "Git timestamps" is the specific evidentiary claim the sentence makes; for these two of the six named files, git timestamps do not show the registration preceded the computation — only the document's own internal dating does (which the sentence doesn't distinguish from the stronger, git-verified claim it makes for the other four).

**6. The record this paper is graded against moved while it was being reviewed — and by extension, will move for any future reader who opens the file Appendix A tells them to open.**

`data/walk_forward/{reads,scores,summary}.json` were overwritten mid-review by two further runs (`walk_20260902T192906Z`, then `walk_20260902T193022Z`), executed by activity outside this review (this review is read-only and never wrote under `data/`). The paper's run `182828Z` is no longer at the path Appendix A names (`data/walk_forward/summary.json`); it survives only in `data/walk_forward/runs/walk_20260902T182828Z/{reads,scores,weights}.jsonl.gz` (`reads`/`scores` recovered and spot-checked in this review — matches captured earlier), and `summary.json` for that specific run is not separately archived by the pipeline at all (only the three `.jsonl` logs are). Every numeric check in section (a) above was performed against content captured during this review before the overwrite; a reader following the paper's own stated protocol ("Numbers from run `walk_20260902T182828Z` (`data/walk_forward/summary.json`) unless a file is named") today would see a different run's numbers at that exact path with no error or warning.

**7. IES-90's "war" label is contaminated by pre-existing wars in a way the paper's own strongest structural claims (§9-§10) do not address.**
> §9: *"The engine's analog selection carries information about the labels."*
> §10: *"The engine knows something about which situations stay quiet and is over-confident about which ones burn."*

Roughly 80% of level-3 (war) IES-90 labels reflect wars already under way before the event (31/38 GED rows had ≥250 deaths in the 90 days *before* the event; 12/16 COW war spells began at or before the event) — established elsewhere in this review's brief and consistent with the paper's own admission that "IES-90's UCDP-GED component is location-based (deaths in the country, not between the actors)" (§11.3). Situation-similarity retrieval will trivially cluster already-at-war dyads together; an already-at-war dyad is disproportionately likely to still carry a level-3 label 90 days later by base-rate construction, independent of any genuine forecasting skill. The paper's null verdict on skill is correct and unaffected, but the two sentences above claim the permutation test's rejection and the Murphy decomposition's resolution term reflect the engine having found something — without ever raising the possibility that what it found is "this dyad is currently at war," a fact available at t, not a forecast.

**8. Minor — the Abstract's baseline count elides which target it applies to.**
> Abstract: *"...compared to four baselines under Diebold–Mariano..."*

True of the protocol in general and of the P (price) target in this run; not true of G (escalation) in this run, which has three baselines (`tiers.daily.G.engine_vs` keys: climatology, frozen, random_analogs) — persistence for G is Amendment B, dated the same day but, per the draft's own front matter, not yet folded into a re-issued run. §7 discloses this correctly ("persistence (price; escalation added by Amendment B)"); the Abstract's unqualified phrasing does not.

**9. Minor — one hard misattribution in `docs/demos/2026.md` (not the paper, but part of the requested demo check).**
> *"The largest atom was Ever Given at +12%."*

The +12.011% atom belongs to `hankuk_chemi_seizure_2021`; `suez_ever_given_2021`'s atom is +8.522%. Everything else in all three demos (hashes, seal-before-lookup ordering, every analog date, every quoted score) reproduces exactly from `reads.jsonl`/`scores.jsonl`.
