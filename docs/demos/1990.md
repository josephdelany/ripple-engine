> **SUPERSEDED — NOT A CURRENT CLAIM.** Demonstration pages built from the superseded event walk. The authoritative documents are [`README.md`](../../README.md) and [`PAPER.md`](../PAPER.md).

# 1990-08-02 — Iraq invades Kuwait

*Regenerated 2026-09-03 from run `walk_20260903T052633Z`, on the escalation target
rebuilt under `OUTCOME_MAPPING.md` Amendment 4. Every number below moved when the target
was rebuilt; the previous version of this page is superseded.*

Sealed read `edd386b624c0…`, `sealed_at` 2026-09-03T05:26:34.754Z; outcome looked up
2026-09-03T05:26:34.755Z. Tier daily, horizon 20 trading days.

**This read is not scored for the gates.** `burn_in_ok` is **false**: only 6 prior events
were in the pool against a registered burn-in of 8 (`n_pool` 6, `n_pool_g` 6,
`n_pool_p` 2). It is shown as what the engine said in 1990, not as evidence about the
engine. The pool shrank from 7 to 6 when the target was rebuilt, so this page is further
below the burn-in than it was.

## What the engine could see

Six prior events, and almost nothing about them. `situation_known_at_t` is empty; actor,
target, conflict_scope and tempo are all `situation_blanked`, and VIX, COT, OVX, credit
stress, the real rate and USD are all in `state_unknown` — in 1990 the market block
barely exists either. Three priors carried escalation weight (`engine.G_atoms`):

| prior event | its IES-90 level | weight |
|---|---|---:|
| Operation Praying Mantis 1988 | 2 · force | 0.359 |
| Iran–Iraq war 1980 | 3 · war | 0.321 |
| Iran Air 655 1988 | 0 · none | 0.244 |

Two atoms carried price weight. That is the whole evidential base for this read, and it
is why the burn-in rule exists.

## What it said

**Escalation at +90 days** (`engine.G`): none **0.264** · threat **0.000** ·
force **0.389** · war **0.347**. `engine.D` is 0.264.

**Brent, 20 days** (`engine.P`): two atoms; p10 −4.87, p50 −4.87, p90 +11.03. With two
atoms the p10 and the median are the same number, which is what a two-point distribution
looks like when it is asked for quantiles.

**Materiality:** `NOT_MATERIAL` on the engine and on all fifteen weightings, against
`materiality.truth = true`. Every item missed the largest oil event of the decade.

## What happened

IES-90 level **3 · war** (`g_closed_on` 1990-10-31), no deal. Brent **+23.60%** over the
horizon, closing 1990-08-30, inside a registered big move (`in_big_move`).

## How it scored (published, not counted)

| | escalation Brier | escalation RPS | Brent CRPS |
|---|---:|---:|---:|
| Climatology | **0.1667** | **0.1667** | **16.54** |
| Random analogs | 0.1667 | 0.1667 | 16.54 |
| Engine | 0.6470 | 0.5653 | 18.78 |
| Frozen engine | 0.6470 | 0.5653 | 18.78 |
| Persistence | 1.8150 | 1.9050 | 23.60 |

Lower is better. The engine lost both halves to climatology, and by a wide margin on
escalation — 0.647 against 0.167. It put 0.264 on "none" for an invasion, because one of
its three neighbours was Iran Air 655, an event that ended in nothing.

Climatology looks strong here for a reason that should be stated rather than admired:
four of the six prior events in this pool ended at level 3, so the base rate said "war"
with 0.667 and happened to be right. With six priors that is not calibration, it is a
small sample agreeing with itself — which is exactly why a read below burn-in is
published and not counted.

Persistence, the rule that beats the engine overall, is the worst of the five here. It
said "level 1, as before" into an invasion.

*Receipts: `data/walk_forward/reads.jsonl` and `scores.jsonl`, run
`walk_20260903T052633Z`, `event_id` `iraq_invades_kuwait_1990`, tier `daily`. Open it on
the desk: `./go` → Walk → click the event.*
