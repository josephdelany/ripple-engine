> **SUPERSEDED — NOT A CURRENT CLAIM.** Demonstration pages built from the superseded event walk. The authoritative documents are [`README.md`](../../README.md) and [`PAPER.md`](../PAPER.md).

# 2001-09-11 — the September 11 attacks

*Regenerated 2026-09-03 from run `walk_20260903T052633Z`, on the escalation target
rebuilt under `OUTCOME_MAPPING.md` Amendment 4. Every number below moved when the target
was rebuilt; the previous version of this page is superseded.*

Sealed read `ef0eb8c43194…`, `sealed_at` 2026-09-03T05:26:37.210Z; outcome looked up
2026-09-03T05:26:37.213Z. Tier daily, horizon 20 trading days. Burn-in satisfied —
9 prior events in the pool (`n_pool` 9, `n_pool_g` 9, `n_pool_p` 5). **This read is
scored and does count toward the gates.**

## What the engine could see

Nine prior Gulf and Iran episodes. `situation_known_at_t` is empty — target,
conflict_scope and tempo are all in `situation_blanked`, and actor, asset_role, COT,
OVX, credit stress, the real rate, USD and propensity are in `state_unknown` — so this
read, like most of them, ran on a partial market block. Eight priors carried escalation
weight (`engine.G_atoms`):

| prior event | its IES-90 level | weight |
|---|---|---:|
| Iran–Iraq war 1980 | 3 · war | 0.196 |
| Operation Praying Mantis 1988 | 2 · force | 0.196 |
| Operation Desert Fox 1998 | 2 · force | 0.196 |
| Desert Storm air campaign 1991 | 1 · threat | 0.170 |
| Iraq invades Kuwait 1990 | 3 · war | 0.148 |
| Iran Air 655 1988 | 0 · none | 0.007 |
| Shah leaves Iran 1979 | 3 · war | 0.007 |
| Iran revolution 1979 | 3 · war | 0.007 |

## What it said

**Escalation at +90 days** (`engine.G`): none **0.008** · threat **0.183** ·
force **0.422** · war **0.386**. The engine put four fifths of its mass on force or war
— which, on a day when the United States had just been attacked, is the read a person
would give. `engine.D` (a negotiated-deal flag) is 0.008.

**Brent, 20 days** (`engine.P`): five atoms; p10 −4.87, p50 −2.84, p90 +23.60.

**Materiality:** `MATERIAL` on the engine and on all fifteen weightings, against
`materiality.truth = true`. Every item called it, correctly.

## What happened

IES-90 level **3 · war** (`g_closed_on` 2001-12-10), no deal. Brent **−29.09%** over the
horizon, closing 2001-10-09, inside a registered big move (`in_big_move`).

## How it scored

| | escalation Brier | escalation RPS | Brent CRPS |
|---|---:|---:|---:|
| Climatology | **0.2716** | **0.2593** | 28.69 |
| Random analogs | 0.2775 | 0.2600 | 28.69 |
| Frozen engine | 0.5795 | 0.4086 | **26.93** |
| Engine | 0.5884 | 0.4132 | **26.93** |
| Persistence | 1.7150 | 0.9050 | 29.09 |

Lower is better. **The engine lost escalation to the base rate by a factor of two and
won price.** Both halves are worth reading slowly.

On escalation it was directionally right and still scored badly, because the outcome was
level 3 and the engine gave level 3 only 0.386 while spreading 0.422 onto level 2.
Climatology, which put 0.556 on level 3 for the flat reason that most prior events in
this pool ended in war, was better calibrated by accident of the base rate. This is the
project's finding on a single read: a confident, sensible-looking distribution can lose
to an unconditional one, and being *nearly* right on an ordinal target is still a loss.
Persistence, which said "level 2, as before", was much worse here — one of the reads
where the dumb rule fails.

On price the engine beat climatology, random analogs and persistence. Its p10 of −4.87
was the right side of a −29% move; nothing in its pool was close to that magnitude, but
its median was negative and climatology's spread was wider and worse.

*Receipts: `data/walk_forward/reads.jsonl` and `scores.jsonl`, run
`walk_20260903T052633Z`, `event_id` `september_11_attacks_2001`, tier `daily`. The seal
is re-verified by `tests/test_demo_911.py` with `walk.verify_seal`. Open it on the desk:
`./go` → Walk → click the event.*
