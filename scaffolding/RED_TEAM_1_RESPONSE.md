# RED_TEAM_1_RESPONSE — remediation work order
Read CLAUDE.md first. Context: docs/red_team_1.md will hold the full attack
(paste it there verbatim, slice R0). The review is high quality; most
attacks are CONCEDED and answered with computation, not argument. Rules:
every result published as computed; the frozen registered record is never
edited; all new analyses are ADDITIVE lenses, committed with receipts;
claims/wording change only AFTER numbers land, with Joe's sign-off.

## R0 — record
Create docs/red_team_1.md: the reviewer's full text, then this disposition
table (attack # → CONCEDE/CONTEST → action → receipt path). Commit before
any recomputation (the order matters for integrity).

## R1 — the standardized headline (attack 1)
Recompute H1 with BMP standardized abnormal returns as THE number: amp,
bootstrap CI, permutation p. The evidence pack, README, and all surfaces
switch to the SAR number regardless of what it shows. Raw |CAR| moves to
a secondary line labeled "unstandardized (subject to vol-clustering)".

## R2 — the real negative control (attack 2)
Build the VIX-matched pseudo-event placebo: for each corpus event, draw K
non-event dates matched to the same VIX-percentile bucket (and excluding
±30d of any corpus event); compute the same |CAR|/SAR split statistic on
pseudo-events. If pseudo-events show "amplification" comparable to real
events, H1's amplification is vol-clustering and must be reported as
unsupported. Publish either way. This replaces label-shuffling as the
headline soundness check (label-shuffle stays, relabeled as an
association check only).

## R3 — dependence honestly (attacks 7, 8)
Regime-block leave-out: recompute H1 (SAR version) dropping, as whole
blocks, (a) 2008, (b) 2020, (c) 2026, (d) all three. Report all four CIs.
Plus winsorized (5%) variant. No verdict language changes until R1-R3
complete; then the pack states plainly which versions survive.

## R4 — the claim-matched corpus (attacks 3, 4, 5)
(i) Add codebook field endogenous_response (policy reactions to ongoing
price moves: the 2008/2020 OPEC cuts etc.) — flag via documented criteria,
Joe reviews the flag list. (ii) Re-run H1-SAR on: full corpus; ex-
endogenous; geopolitical-only subset (conflict_escalation ∪ chokepoint ∪
infrastructure_attack ∪ sanctions). Report all three. The public claim is
re-worded to whatever the subset supports, pending Joe.

## R5 — frozen numbers lead (attacks 9, 10)
Headline block becomes: (1) frozen N=289 registered-corpus number, (2)
pre-2019-threshold OOS number WITH CI, (3) current-corpus tracking number,
clearly labeled third. In-sample median split gets a frozen-threshold
variant everywhere.

## R6 — cross-chain honesty (attacks 13, 14)
CC2: publish outlier-drop CI (ex hormuz_2026, ex venezuela_2019) and add
month-of-year seasonal adjustment to crack CARs (document method). If the
CI crosses zero, CC2 downgrades to SUGGESTIVE in the portfolio and packs.

## R7 — one evidentiary bar (attacks 15, 16)
Codify in EVALUATION: "validated" requires SAR-standardized effect +
regime-block-robust bootstrap CI excluding zero + permutation-FDR. Apply
retroactively to H1, CC2, copper, everything. Downgrade whatever fails.
The mispricing edge's family-exclusion gets one honest paragraph.

## R8 — purpose reframe (attacks 11, 12, 19) [wording, Joe signs off]
Calibration presented as: honest, near-baseline, no demonstrated forecast
edge. NORTH_STAR/README purpose statement rewritten: measurement and
grounding instrument (what happened, what history says, what state we're
in) — NOT a predictive-edge product. The miss-audit's regime_misread
finding gets cited in the H1 pack as an out-of-sample caution. Provenance
language ("$0/keyless/no-fabrication") reframed as integrity property,
never as evidence of correctness.

## R9 — receipts reconciliation (attacks 17, 18)
data/NUMBERS.md: one table — every public number, the N it was computed
on, the commit, the pack. Fix all count inconsistencies across README/
STATE/EVALUATION. Verify the pre-1990 VIX/VXO splice and confirm the
percentile reference window excludes lookahead; document in the pack.

## Deliverable
All slices committed, acceptance green, then an updated
docs/red_team_1.md disposition table with final numbers, and a one-screen
summary for Joe listing: which claims survived, which downgraded, exact
new headline wording awaiting his sign-off. If the fixes kill H1, the
record says so plainly — a published downgrade after adversarial review
is the strongest integrity evidence this project can produce.
