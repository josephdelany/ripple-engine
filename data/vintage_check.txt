============================================================================================
VINTAGE CHECK -- does any headline number depend on REVISED history? (V-Q1)
generated 2026-08-03T20:02:02+00:00
============================================================================================

1. CAN A REGISTRATION-TIME VINTAGE BE OBTAINED HERE?
   ALFRED probe: NOT honored -- the endpoint returns only the CURRENT vintage.
     requested vintage 2020-06-15 -> response column 'INDPRO_20260803' (vintage 20260803)
   -> a registration-time vintage CANNOT be pulled in this environment (simulated FRED
      mirror does not implement ALFRED vintages; real ALFRED does).

2. DID THE DB RETAIN ANY PRIOR VINTAGES (bitemporal as_of)?
   fred.DCOILBRENTEU    rows=  9942  obs_dates with >1 as_of: 0
   fred.DCOILWTICO      rows= 10210  obs_dates with >1 as_of: 0
   fred.VIXCLS          rows=  9242  obs_dates with >1 as_of: 0
   -> as_of == obs_date on every row; NO prior vintage is stored to diff against.

3. REVISION CLASS OF THE HEADLINE INPUTS (the decisive point):
   H1 (+5.0pp VIX-stress amplification of the Brent ripple)
     - Brent spot   (fred.DCOILBRENTEU): market-close
     - WTI spot     (fred.DCOILWTICO): market-close
     - VIX          (fred.VIXCLS): market-close

   VERDICT: every headline input above is a DAILY MARKET CLOSE (spot price / index close).
   Such series are FINAL at publication and are NOT retroactively revised (unlike GDP,
   payrolls, industrial production). Therefore the headline CANNOT depend on revised
   history BY CONSTRUCTION -- the revised-history attack does not apply to it.

HONEST LIMITATION + FLAG:
  * This receipt does NOT fake an empirical vintage diff by comparing today's data to itself.
    Where a real vintage is unobtainable here, the argument is from the input series' revision
    class, which is stated plainly above.
  * fetch_fred_alfred.py stores each requested vintage under its own as_of, but in this
    environment the endpoint returns TODAY's data for any requested date -- so those as_of
    labels would be current data mislabeled as old vintages. It is NOT in the refresh cycle and
    its demo series (INDPRO/PAYEMS) have 0 rows in the live DB, so it does not contaminate any
    headline today. Do not trust it for point-in-time work here without a fix.

Frozen quarterly DB snapshots (src/db_snapshot.py) preserve today's DB so a future rebuild can
be diffed against this quarter -- the reproducibility half of V-Q1 that IS fully available here.
