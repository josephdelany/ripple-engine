# Gate report — PATH Step 8 (the walk), session B, 2026-09-02

Per SESSION_CHARTER §4.3: what is blocked on Joe, stated so the build moves on. Everything below is
already coded and published as computed; nothing here changes a registered gate.

## Gate 1 (Joe): ratify Amendment A to WALK_FORWARD_PROTOCOL.md

The protocol's §0, §1 and §3 still describe G as the "+90d branch label ... corpus-derived". That is
superseded by OUTCOME_MAPPING.md Amendment 1 (sr_outcome_90 retired) and Joe's instruction of
2026-09-02 (IES-90 target; Brier + ranked probability score). Session B did NOT edit the protocol
(the standing order "do not touch any registered file" and the charter's "write the dated amendment"
conflict; the safer reading won). The proposed text is below, verbatim, ready to append. Items A.4–A.5
describe code written before the amendment (commit c2443a0, 12:37) — recorded, not hidden.

> ## Amendment A (2026-09-02) — the G target is IES-90; the ranked probability score; the placebo reference; size-corrected diagnostics
> - **A.1 G target.** The "+90d branch label" of §0, §1 and §3 is replaced by the **IES-90 level** reached
>   in (d, d+90] (0 none / 1 threat or display of force / 2 use of force / 3 war) and the **DEAL flag**, from
>   `event_outcomes` rows with `source = 'ies90'` as defined in OUTCOME_MAPPING.md Amendment 1 (+1.1, and 2
>   when rebuilt). `sr_outcome_90` / `sr_outcome_30` are never a target, a similarity field, or analog
>   evidence. A geopolitical event with `no_independent_outcome` is read and P-scored, never G-scored and
>   never G evidence; its count is published in `summary.json.data_state`. The §1 label audit is now the
>   30-event IES-90 audit (`data/audits/ies90_audit_30.csv`); the §7 audit flag stays false until
>   `data/audits/outcome_audit.json` records a pass.
> - **A.2 G scores.** §3's multi-category Brier (over the four levels) remains the score that drives §5's
>   Hedge losses, the FDR family and the §7 gates; the log score remains. Added and published beside them:
>   the **ranked probability score** over the ordinal levels (Epstein 1969; Murphy 1971) —
>   RPS = Σ_{k=0}^{2} (F_k − O_k)², F and O the cumulative forecast and outcome, in [0, 3] — with the same
>   engine-vs-baseline comparisons, item comparisons and learning curve (`summary.json.tiers.*.G.rps`).
>   The DEAL flag is scored with the binary Brier where the realized flag is known. Making RPS a gate is a
>   further dated amendment, Joe's to make.
> - **A.3 Reliability.** Murphy's three-term identity is exact only for forecasts on the bin-mean values;
>   for continuous mixtures the exact identity carries the within-bin variance and covariance terms
>   (Stephenson, Coelho & Jolliffe 2008). Both are published; `identity_gap` is the residual of the exact identity.
> - **A.4 Placebo reference (§6).** "Engine skill on placebos" is judged against §4 baseline 3 (the same k
>   analogs drawn at random from the class) because it is size-matched: a null engine has zero expected skill
>   against it. The climatology-referenced placebo skill is published beside it (negative under the null by
>   A.5), as is the size-corrected one. `null_holds` is the size-matched interval covering 0.
> - **A.5 Size-corrected diagnostics (not gates).** The registered CRPS and Brier of an empirical
>   distribution built from k analogs exceed the population score by E|X−X′|/(2k) and Σ_b p_b(1−p_b)/k
>   (derived; reproduced by simulation in `tests/test_walk.py`), so the registered skill of a k-atom read
>   against a ~10k-atom climatology is negative under the null by sample size alone. The size-corrected
>   scores (Ferro 2014; weighted form, c = Σw²/(1−Σw²)) are published in every tier's `diagnostic_fair`
>   block and in the placebo, and drive nothing.
> - **A.6 Leakage run.** `FAR_FUTURE` for the broken-filtration scale window is 2200-01-01 (2999-12-31
>   overflowed numpy datetime64[ns] and made the leakage test void); broken-filtration candidates may not
>   cross tiers on the price side.

## Gate 2 (Joe): should §3 adopt the size-corrected scores for the gates?

Evidence, from the published daily tier (registered settings): on P the registered skill vs climatology
is −0.027 (CI −0.061..+0.009) and the size-corrected one +0.027 (CI −0.009..+0.072); the climatology-
referenced placebo is −0.074 (CI −0.105..−0.044) where the size-matched one is −0.022 (CI −0.054..+0.008).
Under the registered rule a P-engine with no skill can never look better than climatology, and a
climatology-referenced placebo can never hold. Options: (a) keep §3 and read every "vs climatology"
number knowing the bias (published beside it); (b) amend §3 to the fair scores for skill and gates.
Session B's recommendation: (b), before any result is called VALIDATED. A one-line switch in
`src/walk.py` (`REGISTERED`) plus a re-run (~4 min) implements either.

## Gate 3 (session A → Joe): the IES-90 audit and the Amendment 2 rebuild

The published walk uses the `ies90` rows as computed 2026-09-02T16:52Z (Amendment 1 + 1.1). When
session A rebuilds them under Amendment 2 (dyadic precedence; littoral map), `python3 src/walk.py`
re-runs the walk on the new labels; reads.jsonl is append-only, so the earlier sealed run stays. The §7
audit flag is false until the 30-event IES-90 audit is recorded.

## For Joe's reading of the published run (walk_20260902T180821Z)

Against IES-90 the engine's G skill vs climatology is null (Brier −0.045, CI −0.136..+0.039; permutation
p = 0.128; SPA p = 0.945, best model = frozen). Against the retired corpus-derived label it had read
+0.116 (p = 0.001): that was the label agreeing with the situation fields it was built from, not
foresight. The RPS vs random analogs (+0.086, p = 0.065) is the only G comparison near significance and
is not a gate. DEAL is worse than its 6.5% base rate (n = 62). The size-matched placebo now fails in the
NEGATIVE direction (−0.066, CI −0.115..−0.022): at VIX-matched non-event dates the frozen retrieval does
worse than random analogs. That is not leakage; it says the situation fields copied onto a non-event date
retrieve analogs whose outcomes are less representative than a random draw. The earlier run (c2443a0) had
it at −0.022 with the interval covering 0; the scored set changed (burn-in now needs IES-90-labelled
analogs: 241 vs 252 reads) and with it the placebo sample. Sensitivity to the placebo sample is itself a
result to read; nothing was re-run to change it.

## Not a gate, for the record

The monthly tier (14 events) never reaches burn-in and describes only (§9). The charter §3 lists P at
+60 trading days (daily) and +12 months (monthly) as targets; the walk scores the registered +20 / +3
horizons; the read's spec-curve horizons are 15/20/25 trading days and 3/12 months
(`read.TIERS[...]["horizons"]`), so +12 m is carried but unscored and +60 td is not yet in the read at
all — next slice for session B, after this gate report.
