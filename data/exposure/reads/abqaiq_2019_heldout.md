# READ — Abqaiq 2019, held out

*`read(exposure) -> distribution` under EXPOSURE_REGISTRATION.md §4 (2026-09-03, commit 22da52f). Generated by `src/read_exposure.py`; reads the blocks and the price series, writes no table.*

## The exposure supplied

| field | value |
|---|---|
| `asset_name` | Abqaiq crude oil processing and stabilization plant (with Khurais field) |
| `asset_type` | processing |
| `operator` | Saudi Aramco |
| `country_iso3` | SAU |
| `capacity_nameplate_kbd` | 7000 |
| `capacity_affected_kbd` | 5700 |

**Searched 80 historical cases across every block.**

## Result: **NO ADEQUATE PRECEDENT**

**0 comparable case(s); §4 requires at least 5.**

The corpus does not contain enough comparable exposure to answer this question. This is a
**first-class result registered in advance**, not an error and not an empty read. The
alternative — widening the search until five cases appear — would answer a different
question and not say so.

### What actually happened to `abqaiq_attack_2019`

Shown *after* the read, and it played no part in it — the case was removed from the
corpus before its own question was asked.

| field | value |
|---|---|
| `days_to_partial_restore` | 3 |
| `days_to_full_restore` | unknown |

**The read could not have told you this, and says so rather than guessing.** That is
the point of the held-out test: an instrument that answered here would be answering
from something other than the evidence.

**What would change it:** more cases of this asset_type reaching a filled capacity_affected_kbd, or a query that names fewer constraints.

### Why the other cases were not comparable

| reason | n |
|---|---:|
| case has no asset_type -- an unknown type cannot be asserted to match | 28 |
| asset_type terminal != processing | 14 |
| asset_type refinery != processing | 12 |
| asset_type pipeline != processing | 10 |
| asset_type chokepoint != processing | 8 |
| asset_type field != processing | 4 |
| case is INVALID under §2 (a numeric without provenance) | 1 |
| excluded_by_caller (held-out) | 1 |
| comparable only on country -- geography is not exposure similarity | 1 |
| asset_type out_of_schema_enum != processing | 1 |

*Every figure here is a historical frequency over a named and counted set of past cases. Nothing in this output is the probability of anything happening.*
