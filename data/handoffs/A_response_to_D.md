# Session A's response to session D's red team (docs/red_team_2.md), 2026-09-02

Scope: the findings that name files session A owns (`src/state/**`, OUTCOME_MAPPING.md, story_read / feed_build /
api_v2 / app.html / acceptance_v2, corpus tooling, the reader). Nothing in the frozen record was edited; every change
is a dated amendment registered before its code (SESSION_CHARTER.md §2.2). Findings on B's and C's files are left to
them and listed at the end.

| # | finding (short) | status | what was done / the answer, with evidence |
|---|---|---|---|
| A5 | level-3 IES-90 labels are mostly pre-existing wars; κ ≈ 0 describes the retired label, not IES-90 | **answered, part fixed** | Correct on both counts. (1) The README sentence quotes the Step 4 κ (retired label); IES-90 has no κ until Joe's audit — `data/audits/outcome_audit.json` (A-5 recorder) is the instrument, D3a in `--dod` reports it. (2) "War ongoing at d" is what Amendment 1.1 registers for COW war spells and GED ≥ 250 deaths in the location; it is a level reached *in W*, not an onset — stated in A1.2 ("an ordinal reached in W, not a change from before W") and `deaths_ged_pre90` is stored for exactly this reading. Amendment 2 (dyadic precedence) already demotes location-only evidence where a pair exists: 29 events dyadic / 155 location (`data/state/ies90_distribution.json` `level_by_basis`). Whether a pre-existing war should count as "escalation" is a definitional question for Joe on the audit sheet, which now carries `basis` and `rule_fired` per row. |
| A7 (ii) | the seven `sr_*` similarity fields carry no vintage and are never blanked | **fixed on A's side; B's read pending** | WORLD_STATE_FRAMEWORK.md Amendment A (dc33623) + Brief A-8 (49f96b1): every situation field carries `knowable_at`; the join drops fields knowable after t — 726 of 786 dropped, 262 of 313 events with no situation field at t (`data/state/situation_knowable.json`). The engine's `SR_MAP` (session B) still reads `events.sr_*`; handoff `A_to_B_2026-09-02_knowable.md` gives B the counts. |
| A7 (i), (iii) | `derived.cot_pct` / `inv_sigma` as_of = obs_date; Big Moves full-history quantile | **not A's files** | `fetch_cot.py` / `derive_signals.py` (ops) and `big_moves.py` (session C's Big Moves / read.py session B). Noted for them; A's own bridge (A-7) stamps CFTC with a 3-day and EIA with a 5-day lag in `state_panel`. |
| B4 | AUTO_ADMIT wrote `joe_decision = "approve"` — indistinguishable from Joe | **fixed** | SITUATION_CODEBOOK_V2.md amendment 2026-09-02 (registered first) then `src/admit_events.py`: the tier now writes `rec_reason = "AUTO-ADMIT recommended … Joe decides"` and leaves `joe_decision` empty; `run()` returns `auto_admitted: 0, auto_recommended: n`. Executed fact unchanged: 0 rows were ever auto-admitted. |
| B7 | the reader emits no event date and no confidence; fallback quality | **fixed (schema), measured** | CLAIM_LEDGER_REGISTRATION.md Amendment 6 (registered first) then `src/reader.py`: `event_date` (ISO or null — never the capture date) and `confidence` (high/medium/low; `fallback` on the regex path) on both schemas and through `cage()`. Measured (Brief A-9, `data/reader_eval/score.json`): model mode class accuracy 0.84, entity F1 0.93 on 100 real headlines; regex fallback 0.49 / 0.70 — the fallback numbers agree with D's 24/30 and 19/30. The desk labels the mode on every item. |
| C3 | pre-1987 sheet unbuilt; only the state set reached `dossier.py` | **answered; A's part built** | The sheet is session B's deliverable (REGISTRATION.md). A built the first pass on the rule's own terms: 181 ICB dossiers 1946–86, 97 admissible (`data/candidates/dossiers_index.json`), and now the post-1987 sheet and dossiers (Brief A-12, `data/candidates/post1987_candidates.csv`, DOSSIER_RULE.md §5). `dossier.py --csv` reads B's sheet the day it lands. |
| C7 | M07's status reads "SUGGESTIVE" without "/ null"; `/api/walk/summary` serves rules unfiltered; no template shows it with the SPA p | **fixed on the desk** | The Walk screen's G verdict card now prints the §7 status verbatim with the SPA p and best model beside it (`src/app.html` loadWalk); the summary endpoint stays unfiltered by design (Brief A-1: the published file, whole). The status string itself is B's `walk.py`. |
| C8 | location-basis contamination when P is empty (JPOA 2013 level 2 from Iran–Pakistan incidents) | **answered; candidate Amendment 3** | By construction of Amendment 2: with no counterparty coded, only location sources can cover W. The remedy is a coded counterparty on those events (Joe's coding), or an amendment that reads sanctions/policy events as `basis: location` with level withheld — proposed, not made. The rows carry `basis: location` and `rule_fired` so the reader sees it. |
| B5 | the pre-1987 sheet shows Joe the P outcome before he admits | **answered** | A's dossiers carry no market outcome (DOSSIER_RULE.md §2: dates, actors, class, two sources) — the outcome-blind route exists; B's sheet columns are B's registration. |
| C4, C5, C6 | materiality thin-class NOISE; hypothetical claims never resolve; flow verdict threshold | **not A's files** | `materiality.py`, `ledger.py` (v2 claim ledger, session C / shared). Listed for the owner. |
| A1–A4, A6, A8, B1–B3, B6, C1, C2, C9, C10 | walk, protocol, Big Moves, README | **not A's files** | Session B (walk, protocol amendments, README prediction paragraph) and C (Big Moves). |

Closed by A: B4, B7, C7 (fixed); A5, A7(ii), C3, C8, B5 (answered with evidence, remedies named).
Open for others: A1–A4, A6, A7(i)(iii), A8, B1–B3, B6, C1, C2, C4–C6, C9, C10.
