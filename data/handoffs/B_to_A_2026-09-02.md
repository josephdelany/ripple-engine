# Handoff: session B -> session A, 2026-09-02 (from the release check, data/gates/release_check_2026-09-02.md)

Four surface fixes in session A's files. Each traces to the published `data/walk_forward/summary.json`
(run_id `walk_20260902T181720Z`). None changes a number; each makes a surface show what the file says.

1. `src/api_v2.py` `/api/walk/summary` reads keys `rps_vs` and `leakage`; `summary.json` has
   `tiers.*.G.rps` and `leakage_test`. The endpoint therefore omits the RPS block and the leakage verdict.
   Its `M` block comes out all-None because it copies `precision`/`recall`/`base` from `tiers.*.M`, whose
   shape is `{engine: {precision, recall, base_rate, n, tp, fp, fn, tn}, frozen: {...}, <item>: {...}}`.
2. `src/story_read.py` `branches()` (via `src/escalation.py`) still shows the retired `sr_outcome_90`
   branch rates (CONTAINED / LIMITED_RETALIATION / WIDENING / RESOLUTION_BY_DEAL) with the label
   "outcomes at +90d are corpus-derived (subsequent corpus events), not source-audited". OUTCOME_MAPPING.md
   Amendment 1 requires any surface still showing them to say "corpus-derived, retired 2026-09-02".
3. `src/story_read.py` `trust()` still reads the old `windows` shape (rows empty) and labels the block
   "as computed by src/walk_forward.py". The new verdict is already in the response; the label and the
   empty rows are stale.
4. `README.md` lines 56-63 describe run 181720Z. The published run is now `walk_20260902T182828Z` (Amendment 2
   labels, 253 scored reads): RPS vs climatology +0.072 (CI -0.008..+0.151); RPS vs random analogs +0.140
   (CI +0.061..+0.219, p 0.001) -- no longer "borderline"; size-matched placebo -0.024 (CI -0.053..+0.007,
   `null_holds: true`; vs climatology -0.081, CI -0.112..-0.048); spec curve 9 of 54 specifications positive
   (not "negative across every registered setting"). Numbers in `data/walk_forward/summary.json`.
