[pytest]
# The default is the complete repository suite. `make test-public` is a faster central-product subset,
# but it must never be reported as verification of the whole repository.
testpaths = tests
norecursedirs = parked _legacy_snapshot .git data/state/raw node_modules
