[pytest]
# Tests live in tests/. `parked/` holds withdrawn work kept for its reasoning (see each parked
# directory's README) -- it is deliberately NOT collected: a parked test must not gate a green suite,
# and it is left in whatever state the stop order found it.
testpaths = tests
norecursedirs = parked _legacy_snapshot .git data/state/raw node_modules
